# Kotlin versions supported by this koin-compiler-plugin release.
# One adapter module per line under koin-compiler-version-adapter/.
# Drives the CI matrix and the support statement in error messages/docs.
# (2.4.10 reuses the kotlin-2.4.0 adapter module — same 2.4-line ABI, verified via tools/abi-check.)
2.3.20
2.4.0
2.4.10
